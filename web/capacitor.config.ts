import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.whatislifegame.app',
  appName: 'What is Life',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;