import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.whatislife.app',
  appName: 'What is Life',
  webDir: 'web/dist',
  bundledWebRuntime: false,
};

export default config;
